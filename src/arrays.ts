const superHeros: string[] = [];
// const power: number[] = [];
// refactoring line 2
const power: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const users: User[] = [];
// or
const allUsers: Array<User> = [];

superHeros.push("shaktiman");
power.push(90000000);
